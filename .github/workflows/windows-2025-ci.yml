name: 'windows-2025 CI'
on:
  push:
    branches: [ 'master' ]
  pull_request:
    branches: [ 'master' ]

jobs:
  windows-build:
    strategy:
      matrix:
        build_type: [ 'Release', 'Debug' ]
        conan_preset: [ 'conan-release', 'conan-debug' ]
        exclude:
          - build_type: 'Release'
            conan_preset: 'conan-debug'
          - build_type: 'Debug'
            conan_preset: 'conan-release'
    uses: 'podumai/containers/.github/workflows/ci.yml@master'
    with:
      os: 'windows-2025'
      build_type: ${{ matrix.build_type }}
      configure_preset: 'conan-default'
      conan_preset: ${{ matrix.conan_preset }}
      compiler: 'msvc'
